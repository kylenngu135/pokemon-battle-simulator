fix it so turn 1 hp is max for both pokemon. Before the first turn ends both pokemon are just set to 0/maxhp, should be currenthp/maxhp.

pokemon still aren't taking recoil damage on moves such as takedown, double edge, volt tackle, etc. First, learn how all of the moves that do recoil damage calculate how much recoil damage they take. Next, look up every recoil damage move and keep a list of it. Finally, when we use an attack, check if the move is a recoil move and calculate how much hp the pokemon that uses that move should lose. Additionally, add a check after a recoil move is used to check whether a pokemon has fainted.

moves like fly should last multiple turns, they shouldn't happen in the same turn. For moves like fly, dive, dig, and bounce, the pokemon should do the following: They disappear into their attack the same turn they use it. While they are disappeared, they cannot be attacked unless by the select few moves that are allowed to hit that specific attack. Next, they will attack the pokemon the following turn of when it was used and then lose their disappearance status.

Add all status indicators, field effects such reflect that only apply to one team below the hp bar for the opposing user and above the hp bar and pokemon name for actual user. For all field effects that effect the entire field such as weather conditions, fields, and hazards, list them at the top right of the player's screen.

Update the UI so that the entire turn plays out before any prompts are given to players to attack, swap, forfeit, or force switch their pokemon after the current pokemon faints.



Write a claude code prompt to test whether the move takedown was actually resulting in a pokemon losing hp after one move interaction.
